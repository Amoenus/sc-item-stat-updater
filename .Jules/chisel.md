## 2026-05-13 - Extracted SCMDB Parsing Monolith  Learning: The SCMDB scrape script contained massive, tightly coupled domain logic (e.g., BFS tree traversal for blueprint chains and CSV formatting). Extracting this domain logic into isolated helper modules with JSDoc is vital to making the parsing layer testable and enforcing strict data structures via @typedef boundaries.  Action: Consistently abstract complex JSON transformation algorithms out of CLI entry points into dedicated testable modules.
