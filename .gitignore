# Raw extracted game data — large, not committed
Data/

node_modules/
*.backup

*.gfx
*.swf
global.ini.*
dist/

# DataCore XML cache — large extracted files, not committed
csv/datacore/.xmlcache/

# Local environment overrides — not committed
.env.local
Engine/*
