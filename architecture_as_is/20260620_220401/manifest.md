# Manifest

Run folder: `architecture_as_is/20260620_220401`

Generated: 2026-06-20 22:04:01 Europe/Riga

| File | Why it exists | Produced by | Commit disposition |
| --- | --- | --- | --- |
| `architecture_as_is.html` | Primary self-contained as-built architecture report. | Manual synthesis following the installed `as-built-architecture` skill. | Review and commit if this architecture snapshot should be retained. |
| `manifest.md` | Required run manifest describing generated artifacts. | Manual synthesis following the installed `as-built-architecture` skill. | Review and commit if keeping the report. |
| `evidence/command-log.md` | Summarizes commands run and outcomes without storing large raw logs. | Manual summary from command outputs. | Review and commit if keeping the report. |
| `diagrams/as-built-flow.mmd` | Mermaid source for the as-built flow diagram. | Manual synthesis from observed source, scripts, and docs. | Review and commit if keeping the report. |

No secrets or raw `.env.local` values are included. Existing dirty files observed before the run were not modified by this analysis.
