{
  "name": "sc-item-stat-updater",
  "version": "1.0.0",
  "description": "Updates item descriptions in global.ini with detailed component stats from CSV data files.",
  "scripts": {
    "?build": "Compile TypeScript sources to JavaScript",
    "build": "tsc",
    "?build:watch": "Compile TypeScript sources in watch mode",
    "build:watch": "tsc --watch",
    "?typecheck": "Run TypeScript compiler to check for type errors without emitting files",
    "typecheck": "tsc --noEmit",
    "?update": "Update global.ini item descriptions with stats from the CSV data",
    "update": "node --env-file-if-exists .env.local --import tsx/esm bin/update-all.ts",
    "?extract": "Extract original global.ini from the game files",
    "extract": "node --env-file-if-exists .env.local --import tsx/esm bin/extract-global-ini.ts",
    "?pipeline": "Run the full pipeline to extract, update, and deploy global.ini",
    "pipeline": "node --env-file-if-exists .env.local --import tsx/esm bin/pipeline.ts",
    "?pipeline:scrape": "Run the full pipeline including scraping both SCMDB and DataCore",
    "pipeline:scrape": "node --env-file-if-exists .env.local --import tsx/esm bin/pipeline.ts --scrape",
    "?pipeline:scrape:datacore": "Run pipeline scraping only DataCore (uses XML cache by default)",
    "pipeline:scrape:datacore": "node --env-file-if-exists .env.local --import tsx/esm bin/pipeline.ts --datacore --skip-unforge",
    "?pipeline:scrape:datacore:force": "Run pipeline scraping only DataCore (forces unforge extraction)",
    "pipeline:scrape:datacore:force": "node --env-file-if-exists .env.local --import tsx/esm bin/pipeline.ts --datacore --force-extract",
    "?scrape:scmdb": "Scrape the Star Citizen Ship Matrix database",
    "scrape:scmdb": "node --env-file-if-exists .env.local --import tsx/esm bin/scrape-scmdb.ts",
    "?scrape:datacore": "Scrape the DataForge XMLs (extracts if needed or cache not found)",
    "scrape:datacore": "node --env-file-if-exists .env.local --import tsx/esm bin/scrape-datacore.ts --all",
    "?lint": "Run Biome linter",
    "lint": "biome lint .",
    "?format": "Run Biome formatter and write changes",
    "format": "biome format --write .",
    "?check": "Run Biome check (lint + format) and write changes",
    "check": "biome check --write .",
    "?check:ci": "Run Biome check in CI mode (fails on issues without writing)",
    "check:ci": "biome ci .",
    "?check:staged": "Run Biome check only on staged files",
    "check:staged": "biome check --write --staged",
    "?check:changed": "Run Biome check only on changed files",
    "check:changed": "biome check --write --changed",
    "?check:architecture": "Verify internal project architecture rules",
    "check:architecture": "node --import tsx/esm scripts/check-architecture.ts",
    "?check:no-generated-churn": "Ensure no generated data has unexpected churn",
    "check:no-generated-churn": "node --import tsx/esm scripts/check-generated-data-churn.ts",
    "?test": "Run the test suite",
    "test": "node --import tsx/esm --test src/**/*.test.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Amoenus/sc-item-stat-updater.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "bugs": {
    "url": "https://github.com/Amoenus/sc-item-stat-updater/issues"
  },
  "homepage": "https://github.com/Amoenus/sc-item-stat-updater#readme",
  "volta": {
    "node": "24.16.0"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "cli-progress": "^3.12.0",
    "csv-parse": "^6.2.1",
    "csv-stringify": "^6.7.0",
    "domhandler": "^6.0.0",
    "piscina": "^5.1.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "@types/cli-progress": "^3.11.6",
    "@types/node": "^25.8.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  },
  "optionalDependencies": {
    "puppeteer": "^25.0.0"
  }
}
