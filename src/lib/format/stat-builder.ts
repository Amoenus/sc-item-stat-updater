/**
 * Compatibility re-export for older imports.
 * Stat-building enrichment helpers now live in `src/enrichment/stat-builder`.
 */
export { resetStatBuilderWarnings, stat } from '../../enrichment/stat-builder';
export type { StatBuilder } from '../../enrichment/stat-builder';
