/**
 * Compatibility re-export for older imports.
 * Item enrichment config contracts now live in `src/enrichment/item-config`.
 */
export type { BaseItemConfig, IssueRecord, ItemConfig } from '../enrichment/item-config';
